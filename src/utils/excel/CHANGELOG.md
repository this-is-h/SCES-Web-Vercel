# Excel 模板映射变更日志

## 2026-03-16

- 新增模板指纹机制：对“总表”“基础分分表”的表头二维数组与合并区域计算指纹，导入导出前自动比对。
- 模板微调兼容策略：当列顺序、字段名、合并区域变化时，自动重建字段坐标映射并继续执行导入导出。
- 未映射列策略：最后一行无法匹配 `student.js personal` 或 `dyf number` 的列，写入未映射列日志，不阻断主流程。
- 审计策略：模板重建与导入事件写入 `operations.jsonl`，包含变化详情、校验和与操作时间。
